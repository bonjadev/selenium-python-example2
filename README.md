# selenium-python-example2
 This demo example shows the functionality of finding a web element (in this case a text link), which WebDriver clicks after opening a website and finding a link. (Chrome WebDriver)
